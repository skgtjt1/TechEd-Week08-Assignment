//we need query strings to sort the posts ascending and descending, maybe filter as stretch goal

export default function Posts() {
  //here I need to get the psots from the database

  return (
    <div>
      <h1>Posts page!</h1>
    </div>
  );
}
